archivesBaseName = 'aperturemap'

// open layers needs the theme directory to be next to the open layers js file
war {

    from("${jsLibDir}/aperture/1.0/lib") {
        include 'theme/**'
        into '/'
    }

}