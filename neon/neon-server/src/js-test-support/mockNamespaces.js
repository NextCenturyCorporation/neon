(function () {
    // these are variables used by the mock libraries and those that would be present in OWF
    window.OWF = window.OWF || {};
    OWF.Eventing = OWF.Eventing || {};
    OWF.Intents = OWF.Intents || {};

    neon.mock = {};
})();
