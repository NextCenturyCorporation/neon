archivesBaseName = 'charts'

war {
    dependsOn 'gruntjs'

    from(jsOutputFile) {
        into 'js'
    }
    from(jsSrcDir) {
        include 'timelinewidget.js'
        include 'barchartwidget.js'
        include 'chartwidget.js'
        into 'js'
    }
}